#ifndef function
#define function
using namespace std;
struct Date
{
	int year;
	int mon;
	int day;
};

//const char* weekday[7];
//const int month_day[12];
//const char* weekday[7]={"Sunday","Monday","Tuesday","Wednesday",
//					"Thursday","Friday","Saturday"};
//const int month_day[12]={31,28,31,30,31,30,31,31,30,31,30,31};

bool isleap(int year);//判断是否是闰年

Date add_day(struct Date date);//增加一天

Date add_mon(struct Date date);//增加一月

Date add_year(struct Date date);//增加一年

int _day_of_week(struct Date date);

Date next_monday(struct Date date);//计算下一星期一

#endif // function
